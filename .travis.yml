sudo: true
language: cpp
compiler:
  - gcc
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-7
      - libsfml-dev
      - libgtest-dev
      - cmake
      - lcov
      - imagemagick
before_install:
  - export CXX=/usr/bin/g++-7
  - pip install --user cpp-coveralls
  - cd /usr/src/gtest
  - sudo cmake CMakeLists.txt
  - sudo make
  - sudo cp *.a /usr/lib
  - cd -
script:
  - mkdir cmake-build-debug && cd cmake-build-debug
  - cmake .. && make && ./runTests
  - cd ../validation_test && ./test.sh
after_script:
  - ../coverage.sh